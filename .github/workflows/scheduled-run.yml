name: Scheduled Run (disabled)
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "This workflow has been disabled in favor of .github/workflows/scheduled-task.yml (GitHub Actions-only)."
          echo "If you want to re-enable scheduled remote POSTs, restore the original workflow and set RUN_URL/RUN_KEY secrets."
